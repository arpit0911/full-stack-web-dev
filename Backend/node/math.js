const sum = (a, b) => a + b;
const mul = (a, b) => a * b;

const student = {
  name: "Arpit SIngh",
  age: 25,
  location: "lucknow",
};

let data = {
  sum: sum,
  mul: mul,
  student: student,
};

module.exports = data;
